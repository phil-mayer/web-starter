#!/usr/bin/env node

let shell = require('shelljs')
let colors = require('colors')
let fs = require('fs') //fs already comes included with node.

// Simple Node.js script to eject all boilerplate repository assets.
run();

async function run() {

}
